---
phase: 01-consolidation-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["Brewfile", "Pacfile"]
autonomous: true
requirements: ["TOOL-01"]
must_haves:
  truths:
    - "Brewfile has category comments mirroring the Pacfile"
    - "CLI tools have strict 1:1 parity between macOS and Linux"
  artifacts:
    - path: "Brewfile"
      provides: "macOS package manifest"
    - path: "Pacfile"
      provides: "Linux package manifest"
  key_links: []
---

<objective>
Align `Brewfile` and `Pacfile` for exact tool parity and consistent categorization.

Purpose: Ensures macOS and Linux environments provide the exact same CLI experience and maintains manifest readability.
Output: Categorized `Brewfile` and updated `Pacfile` with missing tools added.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Categorize Brewfile</name>
  <files>Brewfile</files>
  <action>Restructure the `Brewfile` by adding category comments (e.g. `# === SYSTEM ===`, `# === SHELL ===`) that mirror the structure of `Pacfile`. The file must remain flat so it is compatible with `brew bundle dump`, but visually organized via comments per decision D-03.</action>
  <verify>
    <automated>grep -q "^# ===" Brewfile</automated>
  </verify>
  <done>Brewfile is categorized with comments matching Pacfile categories.</done>
</task>

<task type="auto">
  <name>Task 2: Ensure CLI Parity</name>
  <files>Brewfile, Pacfile</files>
  <action>Cross-reference `Brewfile` and `Pacfile`. Identify any CLI tools (e.g., eza, fzf, ripgrep) missing from one but present in the other and add them to ensure strict 1:1 parity (per D-01). Also review GUI applications where reasonable (per D-02). Use `grep` or standard package search tools if a direct equivalent has a different package name.</action>
  <verify>
    <automated>grep -q "fzf" Brewfile && grep -q "fzf" Pacfile</automated>
  </verify>
  <done>All core CLI tools exist in both manifests.</done>
</task>

</tasks>

<verification>
Check that both `Brewfile` and `Pacfile` contain comments and have matching core tools.
</verification>

<success_criteria>
- Brewfile has comment sections.
- Both manifests have the same core CLI tools.
</success_criteria>

<output>
After completion, create `.planning/phases/01-consolidation-audit/01-01-SUMMARY.md`
</output>