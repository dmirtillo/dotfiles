---
phase: 08-officecli-hybrid-markitdown-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "dot_zshrc.tmpl"
  - "private_dot_config/powershell/user_profile.ps1.tmpl"
autonomous: true
requirements:
  - CORE-03
must_haves:
  truths:
    - "A query wrapper utility correctly links target text to explicit DOM paths."
    - "The workflow can execute end-to-end structural DOM changes (e.g. fill colors) on documents processed through markitdown."
    - "The office-query utility parses JSON and returns a clean text list including the matched text snippet, path, and all other relevant fields."
    - "If text is missing, office-format prints an error and returns a failure code."
    - "If there are multiple matches, office-format formats all of them."
    - "Formatting properties are pre-validated against a known list. Invalid properties block the operation and error out."
  artifacts:
    - "dot_zshrc.tmpl"
    - "private_dot_config/powershell/user_profile.ps1.tmpl"
  key_links:
    - "office-format uses officecli query internally to retrieve the DOM path before applying properties via officecli set."
---

<objective>
Implement `office-query` and `office-format` wrapper utilities to establish a robust hybrid document modification workflow using `markitdown` and `officecli`.

Purpose: Enable users to discover exact DOM paths by searching for text, and apply structural formatting (like fill colors) directly to the parent elements using those DOM paths. Includes strict parameter validation and robust error handling.
Output: Shell functions in Zsh and PowerShell for hybrid workflows.
</objective>

<execution_context>
@/Users/dmirtillo/projects/software/dotfiles/.planning/ROADMAP.md
@/Users/dmirtillo/projects/software/dotfiles/.opencode/skills/officemanagement/officecli.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@dot_zshrc.tmpl
@private_dot_config/powershell/user_profile.ps1.tmpl
</context>

<tasks>

<task type="auto" id="1">
  <name>Task 1: Add office-query and office-format to Zsh profile</name>
  <description>Implement shell functions in dot_zshrc.tmpl to provide query capability and structural DOM formatting with strict validation.</description>
  <files>dot_zshrc.tmpl</files>
  <read_first>dot_zshrc.tmpl, .opencode/skills/officemanagement/officecli.md</read_first>
  <action>
Modify `dot_zshrc.tmpl` under the `Agent CLI Wrappers` section (near `office-read` / `office-write`).
Add `office-query` which takes a file and text string, running `officecli close` to flush resident mode, then `officecli query "$file" ":contains(\"$text\")" --json | jq -r '.data.results[] | "Match: \(.text)\nPath: \(.path)\nType: \(.type)\n---"'`.
Add `office-format` which takes a file, text string, and variadic format properties (`$@`).
- Implement argument pre-validation against a known list of valid formatting properties. If an invalid argument is provided, block the operation and error out.
- Call `officecli query "$file" ":contains(\"$text\")" --json | jq -r '.data.results[].path // empty'` to resolve ALL matching DOM paths.
- If no matches are found, print an error message and return a failure exit code.
- If matches are found, loop through ALL matches and apply formatting to each structural element using `officecli set "$file" "$path" "$@"`.
- Ensure it flushes resident mode at the end.
  </action>
  <verify>
    <automated>grep -q "office-format()" dot_zshrc.tmpl && grep -q "office-query()" dot_zshrc.tmpl</automated>
  </verify>
  <done>Zsh alias functions office-query and office-format are added with validation and multi-match support.</done>
  <acceptance_criteria>
    - `office-query` parses JSON and returns a clean text list with snippets and paths.
    - `office-format` validates format arguments against a known list, blocking on errors.
    - `office-format` applies formatting to all matching paths.
    - `office-format` prints an error and returns a failure code if text is not found.
  </acceptance_criteria>
</task>

<task type="auto" id="2">
  <name>Task 2: Add hybrid workflow wrappers to PowerShell profile</name>
  <description>Implement corresponding office-query, office-format, office-read, and office-write functions in the Windows PowerShell profile with validation.</description>
  <files>private_dot_config/powershell/user_profile.ps1.tmpl</files>
  <read_first>private_dot_config/powershell/user_profile.ps1.tmpl</read_first>
  <action>
Modify `private_dot_config/powershell/user_profile.ps1.tmpl`.
Add `function office-read`, `function office-write`, `function office-query`, and `function office-format` to maintain cross-platform parity.
For `office-query`, use `officecli query $file ":contains(\`"$text\`")" --json | ConvertFrom-Json` and output a formatted list of matches with text snippets and paths.
For `office-format`:
- Pre-validate `$props` against a known list of valid formatting properties. Block and error if invalid.
- Parse JSON with `ConvertFrom-Json` to get all `$paths = $queryResult.data.results.path`.
- If no paths are found, Write-Error and return a failure code.
- Iterate over all `$paths` and invoke `& officecli set $file $path $props` for each.
- Ensure `officecli close` is called.
  </action>
  <verify>
    <automated>grep -q "function office-format" private_dot_config/powershell/user_profile.ps1.tmpl && grep -q "function office-query" private_dot_config/powershell/user_profile.ps1.tmpl</automated>
  </verify>
  <done>PowerShell functions office-query, office-format, office-read, and office-write are added with parity.</done>
  <acceptance_criteria>
    - PowerShell profile contains parity implementations for all 4 `office-*` wrappers.
    - `office-query` returns formatted text with snippets and paths.
    - `office-format` validates arguments, blocks on invalid ones, formats all matches, and errors if no text is found.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shell execution | Command line arguments cross from untrusted input to shell execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Injection | office-format | medium | mitigate | Quote all text input securely and pass arguments exactly to prevent command injection via text queries. Parameter pre-validation mitigates malicious options. |
| T-08-SC | Tampering | npm/pip/cargo installs | high | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
- `grep -q "office-format" dot_zshrc.tmpl`
- `grep -q "office-query" dot_zshrc.tmpl`
- `grep -q "function office-format" private_dot_config/powershell/user_profile.ps1.tmpl`
</verification>

<success_criteria>
The hybrid workflow alias functions (`office-query`, `office-format`) correctly resolve text to explicit DOM paths, validate formatting properties, and run structural updates on all matches, meeting the success criteria for Phase 8.
</success_criteria>

<output>
Create `.planning/phases/08-officecli-hybrid-markitdown-workflow/08-01-SUMMARY.md` when done
</output>
