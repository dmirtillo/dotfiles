---
phase: "07"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - dot_zshrc.tmpl
  - Documents/PowerShell/Microsoft.PowerShell_profile.ps1.tmpl
autonomous: true
requirements:
  - CORE-02
must_haves:
  truths:
    - "AGY_BUSINESS_PAYGO_TIER is exported in the Zsh system profile."
    - "GCP_GE_PAYGO_TIER is exported in the Zsh system profile."
    - "AGY_BUSINESS_PAYGO_TIER and GCP_GE_PAYGO_TIER are exported in the PowerShell system profile for cross-OS parity."
    - "antigravity-cli bypasses the subscription requirement natively via environment configuration."
  artifacts:
    - "dot_zshrc.tmpl"
    - "Documents/PowerShell/Microsoft.PowerShell_profile.ps1.tmpl"
---

<objective>
Implement the PAYGO environment variables for antigravity-cli discovered in Spike 043.
Purpose: Allow antigravity-cli to bypass the subscription requirement natively when used in business PAYGO modes.
Output: Shell templates updated to export `AGY_BUSINESS_PAYGO_TIER` and `GCP_GE_PAYGO_TIER`.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Add PAYGO variables to Zsh profile</name>
  <files>dot_zshrc.tmpl</files>
  <description>Add AGY_BUSINESS_PAYGO_TIER and GCP_GE_PAYGO_TIER to dot_zshrc.tmpl</description>
  <read_first>dot_zshrc.tmpl</read_first>
  <acceptance_criteria>
    - AGY_BUSINESS_PAYGO_TIER is exported in dot_zshrc.tmpl
    - GCP_GE_PAYGO_TIER is exported in dot_zshrc.tmpl
    - The exports are conditionally added inside the `.gcloud_project` check block (around line 258) to align with existing Google GenAI variables.
  </acceptance_criteria>
  <action>
    Modify the `{{ if ne .gcloud_project "" -}}` block in `dot_zshrc.tmpl` to include `export AGY_BUSINESS_PAYGO_TIER=true` and `export GCP_GE_PAYGO_TIER=true`. Ensure the syntax is valid Zsh `export VAR=value`.
  </action>
  <verify>
    <automated>grep -q "AGY_BUSINESS_PAYGO_TIER" dot_zshrc.tmpl && grep -q "GCP_GE_PAYGO_TIER" dot_zshrc.tmpl</automated>
  </verify>
  <done>Zsh profile contains the correct PAYGO exports.</done>
</task>

<task type="auto">
  <name>Task 2: Add PAYGO variables to PowerShell profile</name>
  <files>Documents/PowerShell/Microsoft.PowerShell_profile.ps1.tmpl</files>
  <description>Add AGY_BUSINESS_PAYGO_TIER and GCP_GE_PAYGO_TIER to PowerShell profile for cross-OS parity</description>
  <read_first>Documents/PowerShell/Microsoft.PowerShell_profile.ps1.tmpl</read_first>
  <acceptance_criteria>
    - $env:AGY_BUSINESS_PAYGO_TIER is exported in Microsoft.PowerShell_profile.ps1.tmpl
    - $env:GCP_GE_PAYGO_TIER is exported in Microsoft.PowerShell_profile.ps1.tmpl
    - Added inside a `.gcloud_project` check block (alongside GOOGLE_GENAI_USE_VERTEXAI and GOOGLE_CLOUD_PROJECT to fix the existing parity gap).
  </acceptance_criteria>
  <action>
    Add a new `{{ if ne .gcloud_project "" -}}` block near the top of the PowerShell profile template (e.g., right under the initial header/comments) to set the Google Cloud and PAYGO environment variables, maintaining Windows parity. Use PowerShell syntax `$env:VAR="true"`. Make sure to set `GOOGLE_GENAI_USE_VERTEXAI` and `GOOGLE_CLOUD_PROJECT` there as well to achieve parity with Zsh.
  </action>
  <verify>
    <automated>grep -q "AGY_BUSINESS_PAYGO_TIER" Documents/PowerShell/Microsoft.PowerShell_profile.ps1.tmpl && grep -q "GCP_GE_PAYGO_TIER" Documents/PowerShell/Microsoft.PowerShell_profile.ps1.tmpl</automated>
  </verify>
  <done>PowerShell profile contains the correct PAYGO and GCP exports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| User Shell Environment | Variables dictate API billing/tier paths |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | Shell profile env vars | low | accept | Values are hardcoded template strings controlled by chezmoi, requiring local access to modify. |
</threat_model>

<verification>
Ensure the env vars exist in both shell profile templates under the appropriate chezmoi templated blocks.
</verification>

<success_criteria>
- `AGY_BUSINESS_PAYGO_TIER` and `GCP_GE_PAYGO_TIER` are available when `gcloud_project` is set in both Zsh and PowerShell.
</success_criteria>

<output>
Create `.planning/phases/07-antigravity-cli-byok/07-01-SUMMARY.md` when done
</output>