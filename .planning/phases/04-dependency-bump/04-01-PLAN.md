---
phase: 04-dependency-bump
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Brewfile
  - run_onchange_install-packages.sh.tmpl
autonomous: true
requirements:
  - DEP-01

must_haves:
  truths:
    - "Brewfile reflects opencode version tracking at v1.14.35"
    - "run_onchange_install-packages.sh.tmpl enforces litellm v1.83.14"
  artifacts:
    - path: "Brewfile"
      provides: "opencode installation manifest"
    - path: "run_onchange_install-packages.sh.tmpl"
      provides: "litellm installation and version pinning"
  key_links: []
---

<objective>
Update `Brewfile` and `run_onchange_install-packages.sh.tmpl` to reflect the latest versions of `opencode` (v1.14.35) and `litellm` (v1.83.14).

Purpose: Ensure development environments use the exact tool versions that match the current binaries.
Output: Modified configuration files with pinned/updated versions.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin litellm to v1.83.14 in install script</name>
  <files>run_onchange_install-packages.sh.tmpl</files>
  <action>
    Update the `pipx install` block for litellm.
    1. Change the install command to specify the exact version: `pipx install --force 'litellm[proxy]==1.83.14' --python python3.13`
    2. Update the idempotency check to verify the correct version: `pipx list --short 2>/dev/null | grep -q '^litellm 1\.83\.14'`
    3. Ensure the `--force` flag is used during installation so pipx overrides any existing incorrect version.
  </action>
  <verify>
    <automated>grep -q "1\.83\.14" run_onchange_install-packages.sh.tmpl</automated>
  </verify>
  <done>Idempotency check looks for `litellm 1.83.14` and installation uses `==1.83.14`.</done>
</task>

<task type="auto">
  <name>Task 2: Update opencode version reference in Brewfile</name>
  <files>Brewfile</files>
  <action>
    Update the `Brewfile` to reflect the requirement that `opencode` is tracking `v1.14.35`.
    Add an explicit inline comment or version identifier to the `brew "anomalyco/tap/opencode"` line (e.g. `# tracks v1.14.35` or if syntax is supported `brew "anomalyco/tap/opencode", id: "v1.14.35"`) so that the version requirement is documented and maintained.
  </action>
  <verify>
    <automated>grep -q "1\.14\.35" Brewfile</automated>
  </verify>
  <done>Brewfile references opencode version 1.14.35.</done>
</task>

</tasks>

<verification>
1. `Brewfile` mentions version 1.14.35 for opencode.
2. `run_onchange_install-packages.sh.tmpl` uses `litellm[proxy]==1.83.14`.
</verification>

<success_criteria>
- litellm version check and install use the correct v1.83.14 identifier.
- opencode version target is explicitly defined in Brewfile as v1.14.35.
</success_criteria>

<output>
After completion, create `.planning/phases/04-dependency-bump/04-01-SUMMARY.md`
</output>