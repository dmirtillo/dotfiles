---
phase: 03-powershell-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .chezmoiignore
  - Documents/PowerShell/Microsoft.PowerShell_profile.ps1.tmpl
autonomous: true
requirements:
  - WIN-01
must_haves:
  truths:
    - The PowerShell profile is not deployed to macOS or Linux environments
    - Tool initializations (Oh My Posh, zoxide, fzf) use aggressive caching to ensure sub-50ms startup
    - Oh My Posh loads the 'jandedobbeleer' theme by default
  artifacts:
    - path: "Documents/PowerShell/Microsoft.PowerShell_profile.ps1.tmpl"
      provides: "Native Windows PowerShell profile"
      contains: "Invoke-CachedEval"
    - path: ".chezmoiignore"
      provides: "OS exclusion rules"
      contains: "Documents/PowerShell"
  key_links:
    - from: ".chezmoiignore"
      to: "Documents/PowerShell"
      via: "windows-only exclusion block"
---

<objective>
Establish the foundational Windows PowerShell profile structure and implement aggressive caching for tool initializations.

Purpose: To provide a native PowerShell environment on Windows that mirrors the Zsh setup without compromising the project's strict <50ms startup performance constraint.
Output: A new PowerShell profile template and updated chezmoi ignore rules.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-powershell-parity/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Exclude PowerShell profile on non-Windows</name>
  <files>.chezmoiignore</files>
  <action>
    Update `.chezmoiignore` to ensure the new PowerShell profile is only deployed on Windows.
    Find the `{{ if ne .chezmoi.os "windows" }}` block and add `Documents/PowerShell` to it.
  </action>
  <verify>
    <automated>grep -A 5 'if ne .chezmoi.os "windows"' .chezmoiignore | grep 'Documents/PowerShell'</automated>
  </verify>
  <done>chezmoi ignores Documents/PowerShell on non-Windows systems</done>
</task>

<task type="auto">
  <name>Task 2: Create PowerShell profile with tool caching</name>
  <files>Documents/PowerShell/Microsoft.PowerShell_profile.ps1.tmpl</files>
  <action>
    Create the PowerShell profile template. 
    1. Implement an `Invoke-CachedEval` function (per D-05) that takes a `$Name` and `$Command`. It must:
       - Use `$env:TEMP\ps_cache` as the cache directory.
       - Check if the tool's binary exists using `Get-Command`.
       - Check if the cached `.ps1` file is older than the binary file (`LastWriteTime`).
       - If cache is missing or older, run `Invoke-Expression $Command` and save the output to the cache file.
       - Dot-source (`. `) the cached file.
    2. Add Oh My Posh initialization (per D-04) using `Invoke-CachedEval` and the built-in `jandedobbeleer` theme:
       `Invoke-CachedEval "oh-my-posh" "oh-my-posh init pwsh --config $(oh-my-posh get shell) --config ~/AppData/Local/Programs/oh-my-posh/themes/jandedobbeleer.omp.json"` (or appropriate init command).
    3. Add `zoxide` initialization: `Invoke-CachedEval "zoxide" "zoxide init powershell"`.
    4. Add `fzf` initialization: `Invoke-CachedEval "fzf" "fzf --powershell"`.
  </action>
  <verify>
    <automated>chezmoi execute-template < Documents/PowerShell/Microsoft.PowerShell_profile.ps1.tmpl > /dev/null</automated>
  </verify>
  <done>Profile skeleton exists with functioning caching logic for prompt and navigation tools</done>
</task>

</tasks>

<verification>
- `chezmoiignore` correctly isolates the file to Windows.
- Template syntax is valid Go template (if any `{{ }}` tags used) and parses without error.
</verification>

<success_criteria>
- `Documents/PowerShell/Microsoft.PowerShell_profile.ps1.tmpl` is created and correctly implements `Invoke-CachedEval`.
- Non-Windows platforms explicitly ignore this directory in chezmoi.
</success_criteria>

<output>
After completion, create `.planning/phases/03-powershell-parity/03-01-SUMMARY.md`
</output>