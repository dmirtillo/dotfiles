---
phase: 06-core-spike-knowledge-integration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - dot_zshrc.tmpl
  - run_onchange_setup-gemini.sh.tmpl
autonomous: true
requirements:
  - CORE-01
gap_closure: true
must_haves:
  truths:
    - markitdown reads are configured correctly natively.
    - Gemini CLI ponytail extension installs automatically without blocking.
  artifacts:
    - dot_zshrc.tmpl
    - run_onchange_setup-gemini.sh.tmpl
  key_links: []
---

<objective>
Fix configuration gaps in agent CLI aliases and Gemini setup to ensure smooth end-to-end operation without dependency or idempotency blockers.

Purpose: Allow `office-read` to succeed despite `markitdown` pre-release dependencies, and prevent `chezmoi apply` failures when `gemini extensions install` is run multiple times.
Output: Fixed Zsh configuration template and Gemini setup script.
</objective>

<execution_context>
@/Users/dmirtillo/.config/opencode/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@dot_zshrc.tmpl
@run_onchange_setup-gemini.sh.tmpl
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add prerelease flag to markitdown uv run</name>
  <files>dot_zshrc.tmpl</files>
  <action>
    - Add `--prerelease=allow` to the `uv run` invocation within the `office-read()` function.
    - The new command should be: `uv run --prerelease=allow --with 'markitdown[all]==0.1.6' markitdown "$file"`
    
    Gap reason: `office-read` fails at runtime because `uv run` cannot resolve pre-release dependencies without `--prerelease=allow`.
  </action>
  <verify>
    <automated>grep -q "uv run --prerelease=allow --with 'markitdown\[all\]==0.1.6' markitdown" dot_zshrc.tmpl</automated>
  </verify>
  <done>markitdown reads are configured correctly natively.</done>
</task>

<task type="auto">
  <name>Task 2: Make Gemini extension install idempotent</name>
  <files>run_onchange_setup-gemini.sh.tmpl</files>
  <action>
    - Append `|| true` to the `gemini extensions install` command so it does not fail the script if the extension is already installed.
    - The new line should read: `gemini extensions install https://github.com/DietrichGebert/ponytail --consent || true`
    
    Gap reason: The setup script is not idempotent and fails if the ponytail extension is already installed, breaking `chezmoi apply` due to `set -e`.
  </action>
  <verify>
    <automated>grep -q "gemini extensions install.*|| true" run_onchange_setup-gemini.sh.tmpl</automated>
  </verify>
  <done>Gemini CLI ponytail extension installs automatically without blocking.</done>
</task>
</tasks>

<verification>
grep -q "--prerelease=allow" dot_zshrc.tmpl
grep -q "|| true" run_onchange_setup-gemini.sh.tmpl
</verification>

<success_criteria>
- `office-read` successfully handles pre-release dependencies via `uv run --prerelease=allow`.
- `run_onchange_setup-gemini.sh.tmpl` does not fail when executed sequentially.
</success_criteria>

<output>
Create `.planning/phases/06-core-spike-knowledge-integration/06-03-SUMMARY.md` when done
</output>
