---
phase: 06-core-spike-knowledge-integration
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified: 
  - .chezmoiignore
  - base_template.docx
  - run_onchange_hydrate_office.sh.tmpl
  - run_onchange_setup-gemini.sh.tmpl
autonomous: true
requirements: [CORE-01]
must_haves:
  truths: 
    - Binary office files are ignored by chezmoi templating to prevent corruption.
    - Gemini CLI ponytail extension installs automatically without blocking.
  artifacts: 
    - .chezmoiignore
    - run_onchange_hydrate_office.sh.tmpl
    - run_onchange_setup-gemini.sh.tmpl
  key_links: 
    - setup-gemini passes --consent to bypass interactive prompts.
---

<objective>
Configure template hydration and Gemini extension automation.

Purpose: Ensure binary documents are not corrupted by chezmoi, enable local template hydration, and automate the ponytail extension for Gemini CLI.
Output: Updated .chezmoiignore, hydration script, and a Gemini setup script.
</objective>

<execution_context>
@/Users/dmirtillo/projects/software/dotfiles/.opencode/skills/spike-findings-dotfiles/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ignore binary office files in chezmoi</name>
  <files>.chezmoiignore</files>
  <action>
    Per D-03 (Template Hydration), append `*.docx` and `*.pptx` to the `.chezmoiignore` file.
    This prevents chezmoi from attempting to template binary zip formats.
    // ponytail: simple append to end of file, no complex parsing.
  </action>
  <verify>
    <automated>grep -q "\*.docx" .chezmoiignore</automated>
  </verify>
  <done>Binary file extensions are ignored by chezmoi.</done>
</task>

<task type="auto">
  <name>Task 2: Implement template hydration script</name>
  <files>base_template.docx, run_onchange_hydrate_office.sh.tmpl</files>
  <action>
    Per D-03 (Template Hydration), create a generic `base_template.docx` and a hydration script.
    1. Create a `base_template.docx` containing the exact literal string `PLACEHOLDER_STRING` (you can write a trivial python-docx script to generate this file to avoid binary slop).
    2. Create `run_onchange_hydrate_office.sh.tmpl` that copies `{{ .chezmoi.sourceDir }}/base_template.docx` to `$HOME/Documents/template.docx`.
    3. The script MUST use `officecli set` (or the new `office-write` wrapper) to replace `PLACEHOLDER_STRING` with `{{ .gcloud_project }}` inside the copied document.
    // ponytail: use a simple alphanumeric placeholder without curly braces to avoid Go template escaping hell.
  </action>
  <verify>
    <automated>bash -n run_onchange_hydrate_office.sh.tmpl</automated>
  </verify>
  <done>Hydration script copies and modifies the template via officecli.</done>
</task>

<task type="auto">
  <name>Task 3: Automate Gemini Ponytail installation</name>
  <files>run_onchange_setup-gemini.sh.tmpl</files>
  <action>
    Per D-04 (Gemini Extensions), create a new `run_onchange_setup-gemini.sh.tmpl` script.
    It MUST install the ponytail extension via:
    `gemini extensions install https://github.com/DietrichGebert/ponytail --consent`
    Ensure the `--consent` flag is included to prevent interactive timeouts.
    Add a hash comment at the top for `private_dot_gemini/settings.json.tmpl` so it triggers on config changes.
    Make sure it's executable via `chmod +x`.
    // ponytail: bare minimum bash script for chezmoi run_onchange.
  </action>
  <verify>
    <automated>grep -q "\--consent" run_onchange_setup-gemini.sh.tmpl</automated>
  </verify>
  <done>Gemini setup script installs ponytail non-interactively.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Network → CLI | Downloading and installing Gemini CLI extensions from GitHub |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-03 | Tampering | run_onchange_setup-gemini.sh.tmpl | high | mitigate | Hardcode exact GitHub URL for ponytail extension |
| T-06-SC | Tampering | npm/pip/cargo installs | high | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] packages |
</threat_model>

<verification>
Run `bash -n run_onchange_setup-gemini.sh.tmpl` to verify script syntax.
</verification>

<success_criteria>
- `*.docx` and `*.pptx` are added to `.chezmoiignore`.
- Hydration script dynamically copies and updates the document using `officecli`.
- Gemini setup script automates ponytail extension securely.
</success_criteria>

<output>
Create `.planning/phases/06-core-spike-knowledge-integration/06-02-SUMMARY.md` when done
</output>
