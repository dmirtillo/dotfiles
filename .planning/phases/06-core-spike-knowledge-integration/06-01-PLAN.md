---
phase: 06-core-spike-knowledge-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: 
  - run_onchange_install-packages.sh.tmpl
  - dot_local/bin/executable_office-read
  - dot_local/bin/executable_office-write
autonomous: true
requirements: [CORE-01]
must_haves:
  truths: 
    - The hybrid MarkItDown + OfficeCLI loop is available as native core CLI scripts.
    - markitdown is executed on the fly using uv run without global installation.
  artifacts: 
    - dot_local/bin/executable_office-read
    - dot_local/bin/executable_office-write
  key_links: 
    - office-read flushes resident mode edits using officecli close before reading.
---

<objective>
Integrate recent spike findings into the core CLI scripts for agentic operation, establishing the hybrid read/write pattern.

Purpose: Natively implement the MarkItDown + OfficeCLI loop so agents and scripts can safely read and write office documents without hitting concurrency or resident mode bugs.
Output: office-read and office-write wrapper scripts in ~/.local/bin, and an updated package installer with removed global markitdown installation.
</objective>

<execution_context>
@/Users/dmirtillo/projects/software/dotfiles/.opencode/skills/spike-findings-dotfiles/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update package installer for markitdown</name>
  <files>run_onchange_install-packages.sh.tmpl</files>
  <action>
    <!-- planner-discipline-allow: markitdown -->
    Per D-01 (Hybrid Read/Write Pattern), remove the `pipx` installation of `markitdown` completely without replacing it.
    Find the block checking for `pipx list --short | grep '^markitdown'` and completely delete it, as `uv run` fetches the tool on the fly and does not require a global installation.
    // ponytail: deletion over addition, no installation boilerplate needed.
  </action>
  <verify>
    <automated>grep -v '^#' run_onchange_install-packages.sh.tmpl | grep -c 'markitdown' == 0</automated>
  </verify>
  <done>Installer no longer contains global installation logic for markitdown.</done>
</task>

<task type="auto">
  <name>Task 2: Create office-read script</name>
  <files>dot_local/bin/executable_office-read</files>
  <action>
    Create a minimal wrapper script for reading office files.
    Per D-01, it MUST explicitly call `officecli close "$1"` to flush resident mode changes to disk (ignore errors if file not open) *before* calling `uv run --with 'markitdown[all]' markitdown "$1"`.
    Per D-05 (Multimodal Constraints), do not configure OCR by default to avoid timeouts (standard markitdown behavior without --llm-client).
    Ensure `set -euo pipefail` is used and the script is executable.
    // ponytail: 3 lines of bash, no option parsing needed.
  </action>
  <verify>
    <automated>bash -n dot_local/bin/executable_office-read</automated>
  </verify>
  <done>Script properly closes via officecli before running markitdown.</done>
</task>

<task type="auto">
  <name>Task 3: Create office-write script</name>
  <files>dot_local/bin/executable_office-write</files>
  <action>
    Create a minimal wrapper script for writing to office files via text replacement.
    Per D-01, it MUST execute writes using `officecli set "$1" / --find "$2" --replace "$3"`.
    Per D-02 (Concurrency Safety), it MUST sequentially execute `officecli close "$1"` immediately after to prevent zip corruption.
    Ensure script is executable.
    // ponytail: 4 lines of bash, no option parsing needed.
  </action>
  <verify>
    <automated>bash -n dot_local/bin/executable_office-write</automated>
  </verify>
  <done>Script correctly wraps officecli set and close.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File system → CLI | Local script executing tools against user-provided file paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | executable_office-read | medium | mitigate | Enforce explicit version pinning for markitdown if required to prevent supply chain injection during uv run |
| T-06-02 | Denial of Service | executable_office-write | low | accept | Agent timeout due to blocking sync is acceptable per design constraints |
| T-06-SC | Tampering | npm/pip/cargo installs | high | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] packages |
</threat_model>

<verification>
Ensure bash wrappers pass syntax checks.
</verification>

<success_criteria>
- `run_onchange_install-packages.sh.tmpl` correctly removes global installation for markitdown.
- `office-read` and `office-write` scripts exist and implement the correct execution order.
</success_criteria>

<output>
Create `.planning/phases/06-core-spike-knowledge-integration/06-01-SUMMARY.md` when done
</output>
