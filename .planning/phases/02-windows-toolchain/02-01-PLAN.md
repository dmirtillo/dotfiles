---
phase: 02-windows-toolchain
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Winfile
  - Wingetfile.json
  - run_onchange_install-packages.ps1.tmpl
autonomous: true
requirements:
  - WIN-02
must_haves:
  truths:
    - User can specify packages in a simple text file (Winfile).
    - Windows package installer script uses UAC to elevate if not admin.
    - Script successfully parses Winfile and installs packages via choco or winget.
    - Script executes only when Winfile changes.
  artifacts:
    - path: "Winfile"
      provides: "List of packages to install"
    - path: "run_onchange_install-packages.ps1.tmpl"
      provides: "Installation execution and UAC elevation"
  key_links:
    - from: "run_onchange_install-packages.ps1.tmpl"
      to: "Winfile"
      via: "hash trigger"
      pattern: "\\{\\{ include \"Winfile\" \\| sha256sum \\}\\}"
---

<objective>
Implement plain text Winfile package manifest and native Windows package management execution script.

Purpose: Move away from Wingetfile.json exports to a dotfiles-friendly, plain text manifest (Winfile) mirroring Brewfile/Pacfile, and ensure the execution script handles UAC elevation to properly install via Chocolatey and Winget.
Output: A new `Winfile` manifest, an updated `run_onchange_install-packages.ps1.tmpl` script, and removal of the legacy `Wingetfile.json`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-windows-toolchain/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate to Winfile format</name>
  <files>Winfile, Wingetfile.json</files>
  <action>
    Create a new `Winfile` manifest containing the packages currently listed in `Wingetfile.json` (eza, bat, fzf, zoxide, mise, git, neovim, oh-my-posh, fd, ripgrep). Format it as a simple plain text file, one package per line, and include category comments (e.g., `# CLI Tools`). After creating `Winfile`, delete `Wingetfile.json` (per D-01 and D-02).
  </action>
  <verify>
    <automated>test -f Winfile && test ! -f Wingetfile.json</automated>
  </verify>
  <done>Winfile is created with the correct packages and Wingetfile.json is removed.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Windows installation script</name>
  <files>run_onchange_install-packages.ps1.tmpl</files>
  <action>
    Update `run_onchange_install-packages.ps1.tmpl` to:
    1. Update the chezmoi template hash to trigger on `Winfile` instead of `Wingetfile.json`.
    2. Add a UAC elevation check at the top (if not running as Admin, use `Start-Process -Verb RunAs` to relaunch `$PSCommandPath`, then `exit`) (per D-04).
    3. Determine path to `Winfile` using `$env:CHEZMOI_SOURCE_DIR`.
    4. Loop through the lines of `Winfile`, skipping empty lines and comments (lines starting with `#`).
    5. For each package, attempt to install via Chocolatey (`choco install $pkg -y`). If `choco` fails (or isn't installed), fallback to `winget install --exact $pkg --accept-source-agreements --accept-package-agreements` (per D-03).
  </action>
  <verify>
    <automated>chezmoi execute-template < run_onchange_install-packages.ps1.tmpl | grep -q "Winfile"</automated>
  </verify>
  <done>The script handles UAC elevation, parses Winfile correctly, and attempts choco then winget.</done>
</task>

</tasks>

<verification>
Ensure `Winfile` exists and `Wingetfile.json` is deleted. Ensure the powershell template includes elevation logic and the proper chezmoi template hash trigger.
</verification>

<success_criteria>
- Winfile text manifest exists and contains all packages.
- run_onchange script handles UAC elevation.
- run_onchange script prioritizes Chocolatey with Winget fallback.
</success_criteria>

<output>
After completion, create `.planning/phases/02-windows-toolchain/02-01-SUMMARY.md`
</output>
