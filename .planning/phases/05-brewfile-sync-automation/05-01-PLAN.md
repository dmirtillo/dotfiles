---
phase: 05-brewfile-sync-automation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dot_local/bin/executable_sync-brewfile
autonomous: true
requirements:
  - BREW-01

must_haves:
  truths:
    - "User can run sync-brewfile to update their dotfiles Brewfile"
    - "Untracked packages from the system are appended without modifying existing structure"
  artifacts:
    - path: "dot_local/bin/executable_sync-brewfile"
      provides: "CLI script for syncing packages"
  key_links:
    - from: "dot_local/bin/executable_sync-brewfile"
      to: "Brewfile"
      via: "chezmoi source-path"
      pattern: "chezmoi source-path"
---

<objective>
Implement the `sync-brewfile` bash script to safely merge locally installed Homebrew packages back into the tracked `Brewfile` without destroying comments or structural headers.

Purpose: Automate dotfile dependency tracking while preserving human-readable structure.
Output: An executable Bash script deployed via chezmoi to `~/.local/bin/sync-brewfile`.
</objective>

<execution_context>
@/Users/dmirtillo/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/dmirtillo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-brewfile-sync-automation/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the sync-brewfile script</name>
  <files>dot_local/bin/executable_sync-brewfile</files>
  <read_first>
    - .planning/phases/05-brewfile-sync-automation/05-RESEARCH.md
    - Brewfile
  </read_first>
  <action>Create a new Bash script at `dot_local/bin/executable_sync-brewfile`. Include a standard bash shebang. Use `chezmoi source-path` to resolve the absolute path to `Brewfile`. Run `brew bundle dump --file=- 2>/dev/null` to generate the current system state, and pass both the tracked Brewfile and the system dump into the dual-pass `awk` script shown in `05-RESEARCH.md` (Dual-pass Identity Diffing). If missing packages are found, append them to the Brewfile under a `# NEWLY INSTALLED (UNCATEGORIZED)` header. Run `chmod +x` on the file.</action>
  <verify>
    <automated>bash -n dot_local/bin/executable_sync-brewfile</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n dot_local/bin/executable_sync-brewfile` exits 0 (no syntax errors).
    - File `dot_local/bin/executable_sync-brewfile` contains `chezmoi source-path`.
    - File `dot_local/bin/executable_sync-brewfile` contains `brew bundle dump --file=- 2>/dev/null`.
    - File `dot_local/bin/executable_sync-brewfile` contains the dual-pass awk semantic diffing logic.
    - `ls -l dot_local/bin/executable_sync-brewfile` shows execute permissions (`-rwxr-xr-x` or similar).
  </acceptance_criteria>
  <done>The sync-brewfile script successfully implements semantic dependency tracking without destroying structural file layout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local CLI | Reads local system state and writes to local dotfiles repository |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | executable_sync-brewfile | accept | Runs locally with current user privileges. Homebrew outputs trusted text dump formats. |
</threat_model>

<verification>
- Verify that `bash -n dot_local/bin/executable_sync-brewfile` passes syntax checks.
- Verify that `executable_sync-brewfile` uses semantic `awk` parsing rather than overwriting the entire Brewfile.
</verification>

<success_criteria>
- `dot_local/bin/executable_sync-brewfile` exists and is executable.
- The script implements the requirements for BREW-01.
</success_criteria>

<output>
After completion, create `.planning/phases/05-brewfile-sync-automation/05-01-SUMMARY.md`
</output>