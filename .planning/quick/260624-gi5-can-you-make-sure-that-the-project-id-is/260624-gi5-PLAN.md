---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - private_dot_config/private_opencode/opencode.json.tmpl
  - private_dot_gemini/settings.json.tmpl
  - private_dot_gemini/antigravity-cli/settings.json.tmpl
autonomous: true
requirements: [gi5]
must_haves:
  truths:
    - Project ID is not hardcoded in any template
    - Project ID is explicitly templated to be asked during setup
  artifacts:
    - path: private_dot_config/private_opencode/opencode.json.tmpl
      provides: opencode config with templated project id
    - path: private_dot_gemini/settings.json.tmpl
      provides: gemini settings with templated project id
  key_links: []
---

<objective>
Ensure the project ID is dynamically provided via chezmoi setup prompts and not hardcoded in any template, explicitly mapping it for MCP servers like gcp-cost.
Purpose: Fix potential hardcoding issues and ensure the environment remains fully portable across different machines and users.
Output: Updated templates using `{{ .gcloud_project }}` for project ID fields.
</objective>

<execution_context>
@/Users/dmirtillo/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.chezmoi.toml.tmpl
@private_dot_config/private_opencode/opencode.json.tmpl
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and enforce templated GCP Project ID across configurations</name>
  <files>
    private_dot_config/private_opencode/opencode.json.tmpl
    private_dot_gemini/settings.json.tmpl
    private_dot_gemini/antigravity-cli/settings.json.tmpl
  </files>
  <action>Scan the specified files (and any other `.tmpl` files) for hardcoded project IDs. Confirm that `gcloud_project` is used from `.chezmoi.toml.tmpl` (which prompts the user during setup). Ensure that the `gcp-cost` MCP server in both `opencode.json.tmpl` and `settings.json.tmpl` explicitly receives the project ID via environment variable (e.g., `"GOOGLE_CLOUD_PROJECT": "{{ .gcloud_project }}"`) using the template variable, rather than leaving it empty or hardcoded.</action>
  <verify>
    <automated>grep -q "\.gcloud_project" private_dot_config/private_opencode/opencode.json.tmpl</automated>
  </verify>
  <done>All references to project ID use `{{ .gcloud_project }}` and no hardcoded project IDs exist in the templates.</done>
</task>

</tasks>
