---
phase: 260603-fj7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - private_dot_gemini/antigravity-cli/settings.json.tmpl
  - run_onchange_setup-antigravity.sh.tmpl
autonomous: false
requirements: [FJ7]
must_haves:
  truths:
    - Antigravity CLI receives the templated settings with MCPs
    - Antigravity CLI receives GSD skills
  artifacts:
    - path: private_dot_gemini/antigravity-cli/settings.json.tmpl
      provides: "Templated MCP config"
    - path: run_onchange_setup-antigravity.sh.tmpl
      provides: "Skill installation script"
  key_links: []
---

<objective>
Copy Gemini configurations to Antigravity CLI as Chezmoi templates, including MCPs and Skills.
Purpose: Migrate agent configuration to the new CLI tool smoothly.
Output: Chezmoi template for settings and a setup script to install skills.
</objective>

<context>
@.planning/STATE.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Template Antigravity CLI settings</name>
  <files>private_dot_gemini/antigravity-cli/settings.json.tmpl</files>
  <action>Create a new template `private_dot_gemini/antigravity-cli/settings.json.tmpl`. 
It should merge the existing `~/.gemini/antigravity-cli/settings.json` (`colorScheme`, `enableTelemetry`) and `~/.gemini/settings.json` (`security.auth` vertex-ai, and `mcpServers`). 
Ensure that `mcpServers.aws-pricing` is wrapped in `{{- if hasKey . "aws_profile" }}` and uses `{{ .aws_profile }}` and `{{ .chezmoi.homeDir }}` just like the `opencode.json.tmpl` does. Ensure the file permissions are appropriately secure (by being in `private_dot_gemini`).</action>
  <verify>File exists and contains `mcpServers` with Go template tags</verify>
  <done>Settings template created</done>
</task>

<task type="auto">
  <name>Task 2: Create setup script for skills</name>
  <files>run_onchange_setup-antigravity.sh.tmpl</files>
  <action>Create `run_onchange_setup-antigravity.sh.tmpl`. It should be a bash script that runs `npx get-shit-done-cc@latest --antigravity --global` to install the skills globally for Antigravity CLI. Add a hash comment linking it to the settings file (`# chezmoi:template:hash {{ include "private_dot_gemini/antigravity-cli/settings.json.tmpl" | sha256sum }}`) so it reruns when settings change.</action>
  <verify>Script is executable and contains the `npx get-shit-done-cc` command</verify>
  <done>Setup script created</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Applied chezmoi templates and ran setup for Antigravity CLI</what-built>
  <how-to-verify>
    1. Run `chezmoi apply -v`
    2. Check that `~/.gemini/antigravity-cli/settings.json` correctly contains the MCP configuration with your AWS profile.
    3. Check that the skills have been populated by `get-shit-done-cc`.
    4. Start the Antigravity CLI and verify MCP tools and skills are recognized properly.
  </how-to-verify>
  <resume-signal>Type "approved" if all working fine</resume-signal>
</task>

</tasks>

<verification>
- Template generates correct JSON syntax when applied
- MCP configs use dynamic user profile settings from Chezmoi
- Script is named with `run_onchange_` so it executes properly during apply
</verification>

<success_criteria>
Antigravity CLI configuration and skills are seamlessly migrated and managed via Chezmoi.
</success_criteria>

<output>
Complete execution phase
</output>