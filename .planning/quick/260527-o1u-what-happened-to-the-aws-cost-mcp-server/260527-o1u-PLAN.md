---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: ["AWS-MCP-INVESTIGATION"]
must_haves:
  truths:
    - User understands why the AWS cost MCP server is no longer active by default
    - User knows the steps to re-enable it
  artifacts:
    - path: .planning/quick/260527-o1u-what-happened-to-the-aws-cost-mcp-server/SUMMARY.md
      provides: Explanation of the AWS cost MCP server status
  key_links: []
---

<objective>
Investigate and explain what happened to the AWS cost MCP server in the OpenCode configuration.

Purpose: The user noticed the AWS cost MCP server is missing or changed and needs an explanation of its current state and how to restore it.
Output: A SUMMARY.md explaining the conditional configuration and how to enable it.
</objective>

<execution_context>
@/Users/dmirtillo/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@private_dot_config/private_opencode/opencode.json.tmpl
@.chezmoi.toml.tmpl
</context>

<tasks>

<task type="auto">
  <name>Task 1: Investigate current configuration</name>
  <files></files>
  <action>Check `private_dot_config/private_opencode/opencode.json.tmpl` and the git history (`git log --grep="aws" --grep="mcp"`) to confirm how the `aws-pricing` MCP server is currently configured. Note that in commit f63f5b1 and 227d6c6, it was wrapped in a conditional `{{ "{{" }}- if hasKey . "aws_profile" {{ "}}" }}` block, meaning it is only injected if the user sets an AWS profile in chezmoi.</action>
  <verify>
    <automated>grep 'aws-pricing' private_dot_config/private_opencode/opencode.json.tmpl >/dev/null</automated>
  </verify>
  <done>Investigation completed.</done>
</task>

<task type="auto">
  <name>Task 2: Write explanation summary</name>
  <files>.planning/quick/260527-o1u-what-happened-to-the-aws-cost-mcp-server/SUMMARY.md</files>
  <action>Write a clear explanation to `.planning/quick/260527-o1u-what-happened-to-the-aws-cost-mcp-server/SUMMARY.md` for the user. Explain that:
1. The server was renamed to `aws-pricing` and the runner was changed to `npx -y awslabs.aws-pricing-mcp-server@latest`.
2. It was made conditional. It only renders in `opencode.json` if `aws_profile` is present in their chezmoi config.
3. Provide the command or steps they need to run (e.g., `chezmoi init` or adding `aws_profile` to `~/.config/chezmoi/chezmoi.toml` and running `chezmoi apply`) to get it back.</action>
  <verify>
    <automated>test -f .planning/quick/260527-o1u-what-happened-to-the-aws-cost-mcp-server/SUMMARY.md</automated>
  </verify>
  <done>Summary document created and ready for the user to read.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Chezmoi Data -> Template | The aws_profile key dictates template generation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | Information Disclosure | opencode.json.tmpl | accept | The AWS credentials and profile are managed locally by chezmoi and environment variables. Accept as standard config risk. |
</threat_model>

<verification>
- The SUMMARY.md is created and accurately reflects the git history and chezmoi logic.
</verification>

<success_criteria>
- Clear explanation written in SUMMARY.md about the `aws-pricing` conditional logic.
</success_criteria>

<output>
After completion, output the contents of SUMMARY.md to the user so they can read the explanation.
</output>
