---
phase: quick
plan: "1"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - "QUICK-TASK"
must_haves:
  truths:
    - Docker Desktop is stopped and removed
    - Colima is installed
    - Colima is running
  artifacts: []
  key_links: []
---

<objective>
Stop and remove Docker and Docker Desktop, then install and start Colima.

Purpose: Switch macOS container management to the more lightweight Colima.
Output: Docker Desktop uninstalled, Colima installed and running.
</objective>

<execution_context>
@/Users/dmirtillo/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/dmirtillo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@Brewfile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stop and uninstall Docker Desktop</name>
  <files></files>
  <action>
    Stop the Docker Desktop application if it is currently running (use `osascript -e 'quit app "Docker"'` or `killall Docker`).
    Uninstall Docker Desktop via Homebrew (`brew uninstall --cask docker-desktop --force`).
  </action>
  <verify>
    <automated>! mdfind "kMDItemKind == 'Application'" | grep -i "Docker.app" && ! brew list --cask | grep -q "docker-desktop"</automated>
  </verify>
  <done>Docker Desktop is stopped and removed from the system.</done>
</task>

<task type="auto">
  <name>Task 2: Install Colima and dependencies</name>
  <files></files>
  <action>
    Run `brew bundle install --file=Brewfile` to ensure `colima` and the standard `docker` CLI are properly installed per the project dependencies.
  </action>
  <verify>
    <automated>brew list colima && brew list docker</automated>
  </verify>
  <done>Colima and Docker CLI are installed.</done>
</task>

<task type="auto">
  <name>Task 3: Start Colima</name>
  <files></files>
  <action>
    Start the Colima VM by running `colima start`.
    Once started, ensure that the `docker` CLI can communicate with it (e.g., `docker ps`).
  </action>
  <verify>
    <automated>colima status && docker ps</automated>
  </verify>
  <done>Colima is running and docker commands work.</done>
</task>

</tasks>
